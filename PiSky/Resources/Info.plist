<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDisplayName</key>
	<string>PiSky</string>
	<key>CFBundleName</key>
	<string>PiSky</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
		<false/>
	</dict>
	<key>UILaunchScreen</key>
	<dict>
		<key>UIColorName</key>
		<string>Background</string>
	</dict>

	<!-- The instrument-panel UI was built for a 10.5" tablet; allow every orientation
	     so the PFD / scope / map read correctly in landscape on the 11 Pro Max. -->
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UIStatusBarStyle</key>
	<string>UIStatusBarStyleLightContent</string>
	<key>UIUserInterfaceStyle</key>
	<string>Dark</string>

	<!-- The receiver is reached over plain HTTP on the LAN (192.168.1.207:8088) and
	     the Map screen loads the tar1090 web UI in a WKWebView. ATS must permit
	     cleartext to the local network. Personal sideload on a trusted LAN. -->
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsArbitraryLoads</key>
		<true/>
		<key>NSAllowsLocalNetworking</key>
		<true/>
	</dict>

	<!-- mDNS receiver auto-discovery (NWBrowser / Bonjour _http._tcp). iOS 14+ gates
	     local-network access behind this prompt. -->
	<key>NSLocalNetworkUsageDescription</key>
	<string>PiSky finds your ADS-B receiver on the local network and streams live aircraft data from it.</string>
	<key>NSBonjourServices</key>
	<array>
		<string>_http._tcp</string>
	</array>

	<!-- Hourly tally summary runs as a BGProcessingTask (the iOS analog of the
	     Android HourlyTallyWorker — see docs/PORTING_NOTES.md). -->
	<key>BGTaskSchedulerPermittedIdentifiers</key>
	<array>
		<string>com.pisky.mobile.hourlytally</string>
	</array>
	<key>UIBackgroundModes</key>
	<array>
		<string>fetch</string>
		<string>processing</string>
	</array>
</dict>
</plist>
